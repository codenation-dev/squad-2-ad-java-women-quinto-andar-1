## Como subir as atualizações para o Heroku:

Executar os comandos:

```heroku login```

Dentro da raiz do projeto, executar o seguinte comando:

```git subtree push --prefix backend/errorcenter heroku master```

